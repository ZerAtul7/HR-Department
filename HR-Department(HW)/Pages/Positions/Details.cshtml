@page
@model HR_Department_HW_.Pages.Positions.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Position</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Position.PositionName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Position.PositionName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Position.PositionSalary)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Position.PositionSalary)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && User.Identity.Name == "admin123@gmail.com")
    {
        <a asp-page="./Edit" asp-route-id="@Model.Position.PositionId">Edit</a>
        <span>|</span>
    }
    
    <a asp-page="./Index">Back to List</a>
</div>
